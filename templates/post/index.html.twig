{% extends 'base.html.twig' %}

{% block title %}Orchestra - Feed{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1>Fil d'actualités :</h1>
            <hr>
            <div class="row">
                <div class="col-md-12">
                    {% for post in posts %}
                        <div class="row">
                            <div class="col-md-12">
                            <div class="flex">
                                <h3>{{ post.author.pseudo }}</h3>
                                <p>Le {{ post.createdAt|date('d/m/Y') }}</p>
                                {# {% if category is defined %}
                                <ul>
                                {% for category in post.categories %}
                                    <li>{{ category.name }}</li>
                                {% endfor %}
                                </ul>
                                 {% endif %} #}
                            </div>
                                <p>{{ post.content }}</p>
                                <p><a href="{{ path('app_post_show', { 'id': post.id }) }}">Voir la publication</a></p>
                                <hr>
                            </div>
                        </div>
                    {% endfor %}
                </div>
        </div>
    </div>
{% endblock %}
